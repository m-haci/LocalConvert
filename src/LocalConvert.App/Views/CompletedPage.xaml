<Page
    x:Class="LocalConvert.App.Views.CompletedPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer>
        <StackPanel
            MaxWidth="{StaticResource ContentMaxWidth}"
            Padding="40,48,40,48"
            Spacing="16"
            VerticalAlignment="Center"
            HorizontalAlignment="Left">
            <Border
                Width="56"
                Height="56"
                CornerRadius="28"
                Background="{ThemeResource SubtleFillColorSecondaryBrush}">
                <FontIcon Glyph="&#xE73E;" FontSize="24" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <TextBlock x:Uid="Completed_Title" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock Text="{x:Bind ViewModel.FileName, Mode=OneWay}" Style="{StaticResource SubtitleTextBlockStyle}" TextWrapping="Wrap" />
            <TextBlock Text="{x:Bind ViewModel.FileSize, Mode=OneWay}" Opacity="0.8" />
            <TextBlock Text="{x:Bind ViewModel.DurationText, Mode=OneWay}" Opacity="0.8" />
            <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,12,0,0">
                <Button x:Uid="Completed_OpenFile" Command="{x:Bind ViewModel.OpenFileCommand}" Style="{StaticResource AccentButtonStyle}" />
                <Button x:Uid="Completed_OpenFolder" Command="{x:Bind ViewModel.OpenFolderCommand}" />
                <Button x:Uid="Completed_ConvertAnother" Command="{x:Bind ViewModel.ConvertAnotherCommand}" />
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
